# Core / app
streamlit==1.39.0
pandas==2.2.3
numpy==2.1.2
scikit-learn==1.5.2
matplotlib==3.9.2
plotly==5.24.1

# Models & boosting
xgboost==2.0.4
lightgbm==4.4.1

# Explainability
shap==0.46.0
lime==0.2.0.1

# Fairness & mitigation
fairlearn==0.10.0
aif360==0.6.1

# Imbalanced data handling
imbalanced-learn==0.12.4
smote-variants==0.4.0

# Hyperparameter tuning / experiments
optuna==3.2.0
ray==2.6.0           # optional for distributed tuning with Optuna/Ray

# Optional: neural nets & federated / privacy
torch==2.2.0         # for PyTorch-based models and adversarial debiasing
tensorflow==2.13.0   # required by some AIF360 modules (optional if you avoid AIF360)
flower==1.7.0        # federated learning orchestrator (optional)
diffprivlib==0.7.0   # IBM Differential Privacy library (optional)

# Utilities / deployment
joblib==1.3.2
black==24.3.0        # code formatting (dev)
pytest==7.4.0        # tests (dev)
python-dotenv==1.0.0

# Note: versions chosen for reasonable cross-compatibility; adjust for your environment.
